import React from 'react'
import {Edit, SimpleForm, CreateProps} from 'react-admin';

const SiteEdit = (props: CreateProps) => {
    <Edit {...props} >
        <SimpleForm>
            
        </SimpleForm>
    </Edit>
};

export default SiteEdit;