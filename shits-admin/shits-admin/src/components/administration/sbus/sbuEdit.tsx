import React from 'react'
import {Edit, SimpleForm, CreateProps} from 'react-admin';

const sbuEdit = (props: CreateProps) => (
    <Edit {...props} >
        <SimpleForm>
            
        </SimpleForm>
    </Edit>
);

export default sbuEdit;